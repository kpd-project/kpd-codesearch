# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_WHITELIST_USERS=

# OpenRouter
OPENROUTER_API_URL=https://openrouter.ai/api/v1
OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemini-2.0-flash-001

# Embeddings (OpenRouter: openai/text-embedding-3-small)
EMBEDDINGS_MODEL=openai/text-embedding-3-small
EMBEDDINGS_DIMENSION=1536
# Макс. символов в одном API-запросе (~2k токенов по умолчанию)
EMBED_MAX_CHARS_PER_BATCH=8000

# Qdrant
QDRANT_URL=https://qdrant.gotskin.ru
QDRANT_API_KEY=
# Размер батча upsert (обход 413 nginx)
QDRANT_UPSERT_BATCH_SIZE=200

# Repos
REPOS_BASE_PATH=d:/kpd-project
REPOS_WHITELIST=kpd-backend,kpd-frontend,kpd-se,kpd-landing,kpd-pdf-2

# RAG Agent (generator.py)
# Макс. итераций агентного цикла (поиск → LLM → tool_call → ...)
RAG_AGENT_MAX_ITERATIONS=6
# Макс. символов чанка (0 = передавать целиком)
RAG_CHUNK_DISPLAY_CHARS=0
# Топ-K результатов по умолчанию при вызове search_code
RAG_SEARCH_TOP_K=5
# Верхняя граница top_k
RAG_SEARCH_TOP_K_MAX=15
# Порог score (ниже — отбрасывать, 0 = все)
RAG_MIN_SCORE=0.5
# Макс. результатов при поиске по всем репо
RAG_SEARCH_ALL_LIMIT=15
# max_tokens в цикле агента (с tool_calls)
RAG_AGENT_MAX_TOKENS=3000
# max_tokens для финального ответа
RAG_AGENT_FINAL_MAX_TOKENS=2000
# temperature для LLM (0–1)
RAG_AGENT_TEMPERATURE=0.2
# Таймаут HTTP к OpenRouter (сек)
RAG_AGENT_TIMEOUT=60
# Длина превью tool_call в session log
RAG_LOG_RESULT_PREVIEW_LEN=300
